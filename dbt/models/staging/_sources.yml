version: 2

sources:
  - name: raw
    database: flight_pricing  
    schema: raw
    tables: 
      - name: flights
        description: "Raw flight ticket data"
        columns:
          - name: LEGID
            description: "Unique identifier for each flight ticket"
          - name: SEARCHDATE
            description: "Date when ticket was scraped"
          - name: FLIGHTDATE
            description: "Date of actual flight"